
  # Adventure Photo Gallery

  This is a code bundle for Adventure Photo Gallery. The original project is available at https://www.figma.com/design/tzNA39tPlsagUR0jLAKeJa/Adventure-Photo-Gallery.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  